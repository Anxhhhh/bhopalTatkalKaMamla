# Bhopal Tatkal Backend Configuration
# Port on which the express server runs (default: 5000)
PORT=5000

# (Optional) Google Maps API Key for the frontend configuration endpoint
# Leave blank to fallback to development keyless maps mode
GOOGLE_MAPS_API_KEY=
